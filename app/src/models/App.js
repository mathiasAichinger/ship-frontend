function App (id, key, name, description, iconUrl, lane_templates, builds) {
    this.id = id;
    this.key = key;
    this.name = name;
    this.description = description;
    this.iconUrl = iconUrl;
    this.lane_templates = lane_templates;
    this.builds = builds;
}
